def reverseLinkedList(list1) :
    # iterate through the list
    # curr represents "current" element
    # temporary represents one element down (.nextNode,)
    # prev is the previous element
    # switch curr to point to prev
    # prev moves to curr, curr points to temp, temp goes to next Node
    # for last node, turn temp to face curr then return that as the head
    
